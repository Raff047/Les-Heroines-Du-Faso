import 'package:flutter/material.dart';

class DeleteManagerScreen extends StatelessWidget {
  const DeleteManagerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Delete manager'),
      ),
    );
  }
}
