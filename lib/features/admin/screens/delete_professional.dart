import 'package:flutter/material.dart';

class DeleteProfessionalScreen extends StatelessWidget {
  const DeleteProfessionalScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Delete Professional'),
      ),
    );
  }
}
