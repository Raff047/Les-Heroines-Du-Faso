import 'package:flutter/material.dart';

class DisableManagerScreen extends StatelessWidget {
  const DisableManagerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Disable manager'),
      ),
    );
  }
}
