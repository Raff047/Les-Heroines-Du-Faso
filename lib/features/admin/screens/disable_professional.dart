import 'package:flutter/material.dart';

class DisableProfessionalScreen extends StatelessWidget {
  const DisableProfessionalScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Disable Professional'),
      ),
    );
  }
}
